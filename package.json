{
  "name": "bank-it-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "artifacts/api-server",
    "artifacts/bankit-web",
    "artifacts/mockup-sandbox"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  }
}